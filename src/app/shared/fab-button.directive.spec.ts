import { FabButtonDirective } from './sidenav.directive';

describe('FabButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new FabButtonDirective();
    expect(directive).toBeTruthy();
  });
});
